Backend/India/firebase_key.json


# Private keys and local secrets
Backend/India/private_key.pem
private_key.pem

# Environment variables (local configuration, never commit)
.env
.env.local
.env.*.local

# Frontend local Firebase config (keep sanitized config out of history)
